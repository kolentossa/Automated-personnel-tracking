# Models

Place local inference models here. Examples:

- `yolov8n.onnx`
- `yolov8n.pt`
- `model.rknn`

Do not train on the RK3588 board. Train and export models on a workstation, then copy inference artifacts into this directory.
