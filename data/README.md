# Data

Demo videos live here. `scripts/generate_sample_video.py` creates `sample.mp4`, a synthetic video with anonymous moving shapes for testing without a camera.

Do not store raw camera recordings here unless you have an explicit privacy-approved reason.
